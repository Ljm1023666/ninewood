# 2026-07-07

## Ninewood 大规模种子数据生成（收尾 & 质检）

交付目录：`D:/ninewood/seed-data/`，生成器：`D:/ninewood/scripts/generate_massive_seed.py`（Python，seed=20240707 可复现）。

### 今日完成的修复
1. **重复标题率 15.08% → 0.00%**：给非游戏分支 `make_demand()` 补上 `unique_title()` 全局去重守卫（游戏分支此前已有）。
2. **标签采样强保留核心词**：
   - 游戏需求：强保留 `游戏名 + 代练/陪玩`（路径命中锚点），再从 extras 补足 2-6 个 → 游戏标签合规 836/1025 → **1025/1025**。
   - 普通需求：强保留模板第一个主标签（最贴近标题核心词）→ 标题核心词命中 **20/20**（原 18/20）。
3. **taxonomyLeafId 全量校验**：交叉比对前端 `client-react/src/components/card-pool/taxonomy.ts`（725 节点 / 561 叶子）。修正 4 个误用「非叶子内部节点」的 ID：
   - `ofev-makeup` → `ofevm-stage`；`ofhck-hood` → `ofhckh-standard`；`olgbp-kill` → `olgbpk-100`；`olgbwr-star` 直接删除（其真实叶子 `olgbwrs25-*` 已在列表中）。最终 **157 个使用 ID 全部为合法叶子 ✅**。
4. **报告价格分桶标签 bug 修复**：原 `100` 桶误标为「≤100」，改为区间标签（≤100 / 100–500 / 500–1000 / 1000–5000 / 5000–20000 / >20000）。

### 最终质检全绿
- tags 1162 / keywords 1282 / users 801(含1 ADMIN) / demands 10000。
- 字段缺失 0；无 `paths`、无 `userId` 字段；`tagsConfirmed` 全 true；tag 数全在 2-6；tag 无 `.`+`#` 非法字符。
- status：ACTIVE 8623 / PENDING 1100 / COMPLETED 277，无 FROZEN/IN_PROGRESS/CLOSED（路径检索不过滤）。
- 类目覆盖 30/30；手机唯一；普通用户 serviceTags 2-8。

### 注意点（给下游）
- demand 未带 userId（必填外键），导入时从已导入 User 随机绑一个。
- 路径由接入方调用 `resolveDemandPaths()` 派生，本数据不输出 paths、不写 SQL/Prisma。
- 若需写 `seed-massive.ts` 接入脚本，用户尚未要求，待其提出再补。

### 技术坑（复用）
- Python 脚本在 bash 中需用 Windows 原生绝对路径：`/c/Users/19617/.workbuddy/binaries/python/versions/3.13.12/python.exe D:/ninewood/scripts/xxx.py`（forward slash 即可，反斜杠会被 Git Bash 吞掉）。
