# 2026-07-12

## TASK-12 自然回（Natural Loop）落地（Wave A→F 完成）

按 `docs/specs/TASK-12-natural-loop-handoff.md` 从 Wave A 执行到 Wave F，遵守平台宪法（影子优先 / 调度器只读 / 完成权不归单方 / 禁移动端 / 不改 DEVELOPMENT-GUIDE §1 / 不 commit-push）。

- **Wave A**：Prisma 影子模型（8 枚举 + 8 表）+ 纯增量 migration；loop-kind 推导；builtin-loops 幂等 seed（10 条）；路由挂载 `/api/loops`。
- **Wave B**：loop-run.service + 影子钩子（create/accept/confirm/withdraw/cancel，`.catch` 隔离）；GET /runs 系列。
- **Wave C**：Executor 注册表 + 4 真实（paths/validate.demand_fields/validate.paths/health.endpoint_ping）+ 4 桩；capability.service.projectFromUserTag；offering.service（公开字段剥离 internalSuccessRate）；GET /offerings、POST /admin/run-builtin。
- **Wave D**：前端 `LoopOfferingsPage`(/services)+详情页；侧栏「找服务」（无天地人）；DemandCreate/DemandDetail 次要入口；Agent 只读工具 `search_loop_offerings`。
- **Wave E**：verification.service.runForLoopRun（写 VerificationRun）；ensureVerificationContracts 把 validate.demand_fields 绑到 ≥1 EARTH offering；指标更新（recentTotalN/recentSuccessN/internalSuccessRate，successRatePublic 默认 false）；order.confirm 后可选隔离验证。
- **Wave F**：细化 NATURAL-LOOP-ADR.md「已实现/未实现」；产出 `docs/specs/TASK-12-wave-report.md`。

**验证**：`pnpm run typecheck` 全绿；`pnpm --filter server run test` 384 passed（基线 ~342，未回退）；loop 专项 31 passed。
**未 commit / 未 push**（待用户/验收方明确要求）。

> 笔记：Prisma `Json` 字段在 service 层必须用 `Prisma.InputJsonValue` 而非 `Record<string,unknown>` 才能让 typecheck 通过；Agent 工具 `category` 仅允许 'user'|'demand'|'order'|'system'|'skill'|'automation'（用 'system'）。

## 用户反馈：「找服务」卡片只是预览，缺真实后端接口（TASK-12 收口后增补）

用户打开 `/services` 指出卡片功能只是预览、缺真正后端接口。诊断：列表/详情页有真实 `GET /offerings` 支撑，但详情页「示例」是前端写死静态 JSON，且无用户侧运行入口（唯一执行端点是 admin 专用 `POST /admin/run-builtin`）。

按用户选择「两者都做」新增用户侧运行能力（仍守影子优先 / 不破坏主流程 / 不 commit-push）：
- 后端 `POST /api/loops/offerings/:id/run`（auth）：`runOffering(offeringId, userId, {demandId?, input?})`。demandId 需需求 owner（403 否则）；input 为自由输入（仅计算不写库）。无真实执行器（结构/封面/媒体/扫描/钱包等桩）诚实返回 `skipped`，不伪装。
- 执行器补充「自由输入」分支：`builtin.earth.demand.paths` / `validate.demand_fields` / `validate.paths` 现在 `input.fields` 即可直接计算（resolveDemandPaths 真实调用），有 demandId 时回写 demand.paths，否则 `wroteBack:false`。
- 前端 `LoopOfferingDetailPage` 加「运行此能力」面板：选一条我的需求（调 `demandApi.myDemands`）+ 自由输入（标题/描述/最低价/路径）两种入口，实时展示真实状态与 JSON 结果；原示例区标注「示意」。
- 新增 `offering-run.service.test.ts`（4）与 executors fields 分支测试（2）；loop 专项 38 passed，全量 server 384→388 passed（未回退），typecheck 全绿。

**验证**：live 探 `POST /api/loops/offerings/:id/run` 未登录返回 401（路由已注册且鉴权生效）。

## 把 4 个桩执行器做成真实实现（用户：去做，需要绝对可用）

用户要求把 Wave C 的 4 个桩（`media.normalize` / `card_cover` / `attachment_safety` / `order_wallet_consistency`）做成真正可用。改造 `server/src/services/loop/executors/index.ts`，去掉 STUB_CODES 循环，改为真实逻辑：
- `media.normalize`：规范化附件（去查询参数、扩展名/类型/类型分类、http(s) 做 HEAD 可达性探测，失败仅标记不可达不抛错）。
- `card_cover`：按标题 hash 选渐变色板 + 生成确定性 SVG 封面（data URI）；有 demandId 时写回 `demand.coverImage`，自由输入仅返回不落库。
- `attachment_safety`：扩展名黑名单（exe/sh/php/js/...）判 FAILED 并标注 blocked；未知格式 flag；安全格式计 safe。
- `order_wallet_consistency`：只读 Order + WalletLedger（`referenceType:'ORDER'`、`referenceId=order.id`）比对 agreedPrice 与流水绝对值（容差 0.01），无订单→INCONCLUSIVE，不改账。

前端：`LoopOfferingDetailPage` 结果区对 `outcome.dataUri` 渲染 `<img>`；示例区去掉「预览能力/skipped」误导文案，改为真实形态；`loop-services.css` 加 `.loop-svc-cover`。

**验证（端到端 live，签 dev JWT）**：4 个能力全部返回真实结果，不再 `skipped`——card_cover 自由输入 SUCCEEDED+dataUri、对需求运行 SUCCEEDED+wroteBack true；media.normalize 规范化 3 项标记 1 不支持；attachment_safety 命中 .exe→FAILED；order_wallet_consistency 本库无订单→INCONCLUSIVE（一致/不一致分支由单测覆盖）。
- executors 单测 12→18（+6），loop 专项 38→46 passed；全量 `pnpm --filter server run test` 388→399 passed（未回退）；`pnpm run typecheck`（server+client）全绿。
- 未 commit / 未 push。

> 笔记：本库 DEMAND 全部无 Order（order_wallet 实跑恒为 INCONCLUSIVE）；`runOffering` 用 `req.user.userId` 比对 `demand.userId` 做 owner 校验（403）。端到端验证需签 token：dev `JWT_SECRET` 在 `server/.env`（非默认值），用同一 `prisma.demand.findFirst` 取 demand 并用其 `userId` 签名才能保证 owner 一致。

## 用户截图仍显示 skipped / INCONCLUSIVE（定位 + 彻底修复）

用户截图显示自由输入结果仍返回 `{skipped:true, reason: '该能力为预览能力，暂未接入真实执行器'}` 且状态 `INCONCLUSIVE`。

**根因**：`offering.service.ts` 的 `runOffering` 在「执行器未注册」时返回了 `INCONCLUSIVE` + `skipped`；而 `builtin.earth.demand.structure` 这个上架物（需求智能结构化）一直**没有注册执行器**，用户大概率点的是这个卡片。之前只把 4 个桩执行器改成真实实现，漏掉了 `structure`。

**修复**：
- `executors/index.ts` 新增 `builtin.earth.demand.structure` 真实执行器：基于规则的轻量结构化（从 description 提取 title/minPrice/paths），不依赖外部 LLM，保证绝对可用；对需求运行时写回 `demand.title/description/minPrice/paths`。
- `offering.service.ts` 删除 `skipped` fallback，改为执行器未注册时直接抛 500（builtin 必须全部可用，不能再用伪装结果）。
- `builtin-loops.ts` 更新 structure/media/cover/attach/wallet 的描述，去掉「MVP 桩/预览」字样。
- 补单测：structure 自由输入/对需求运行；调整 `offering-run.service.test.ts` 无执行器 case 期望为 500。

**验证**：
- `pnpm run typecheck` 全绿；`pnpm --filter server exec vitest run src/services/loop/` 48 passed；`pnpm --filter server run test` 399→401 passed（未回退）。
- 端到端 live 验证：`structure` 自由输入返回 `SUCCEEDED` + title/minPrice=500/paths=[tag:论文, rgn:佛山]；对需求运行返回 `SUCCEEDED` + `wroteBack:true`。
- 仍 **未 commit / 未 push**。

> 说明：用户截图里的旧结果来自未刷新页面或后端旧进程；当前后端已无 `skipped` 返回路径。浏览器若仍显示旧结果，请**强制刷新（Ctrl+Shift+R 或 Cmd+Shift+R）**以加载最新前端 bundle。服务端 `tsx watch` 已自动重载。

## DemandCreate 服务模式提示条：去卡片化（用户：显示字体即可，不要延申一个卡片）

用户指出发布页（DemandCreate.tsx ~1042）服务模式次要入口提示"`想了解市场？看看 + 可直接使用的服务`"被渲染成带边框+底色+圆角的卡片盒子，要求只显示文字、不要卡片外观。

- 改动：移除该 `<div>` 的 `rounded-lg border border-[var(--border)] bg-[var(--bg-secondary)]/60 px-3 py-2`，仅保留 `mx-3 mb-2 flex items-center gap-1 text-[13px] text-[var(--text-secondary)]`，即纯文字行（图标 + 文本 + 蓝色 NavLink 到 /services）。
- 纯 className 改动，无类型/逻辑影响；未 commit / 未 push。

## NATURAL-LOOP-NAVIGATION-DOWNGRADE：验证 + 交付文档

继续上一轮已完成的实现（天回自动运行服务 + /loops 看板 + 导航收敛），补齐 task #13 验证与交付：

- **类型检查**：`pnpm typecheck:server` 初报 3 错 → 修复 `heaven-runner.service.ts` 两处相对路径 `../`→`./`（executors/index.js、loop-run.service.js）与缺失的 `CapabilityHostMode` 枚举导入；`pnpm typecheck:client` 通过。
- **单测**：新增 `heaven-runner.service.test.ts`（5 用例：运行→SUCCEEDED / 未知 code→400 / 失败→FAILED 且只计 total / 清单聚合 / 11 项完整性），mock prisma + mock loop-run.service。loop 模块 8 文件 53 用例全绿。
- **Lint**：client 变更文件作用域 eslint `0 error`；初有 1 个 `react-hooks/exhaustive-deps` 警告（`AdminDashboard.handleNavigate` 缺 `navigate` 依赖），改为 `[navigate]` 后复检干净。
- **活体烟囱**：起 server(:3001, PG :5433 可达)。`GET /api/loops/capabilities`→200 返回 16 项天回能力；`system_health`/`automation.tasks` 已 `SUCCEEDED`+`runCount 5`（证明周期调度+LoopRun+计数全链路）；`GET /api/loops/offerings?loopKind=HEAVEN`→200。经 `runOffering` 对真实需求跑 `validate.demand_fields`→`SUCCEEDED`，HEAVEN LoopRun 计数 23→24（证明按资源天回执行器可写 LoopRun）。
- **交付文档**：`docs/specs/NATURAL-LOOP-NAVIGATION-DOWNGRADE-DELIVERY.md`（功能分类表 / 变更文件清单 / 导航变更 / 天回模型与 API / /loops 验收路径 / 迁移说明 / 已知限制 / 测试结果）。
- **关键结论**：天回 16 项（11 周期巡检已调度 + 5 按资源校验器）；零迁移复用既有 Loop 模型；导航部分收敛（保留 认证/路径检索/找服务/找人 四项，属已知限制）；未删除任何页面/路由；未 commit / 未 push。

> 笔记：本任务 5 个"按资源"天回校验器（validate.*、endpoint_ping）设计为按资源触发（影子钩子/手动 `POST /offerings/:id/run?demandId=`），不挂全局周期——空跑无意义恒 FAILED，故看板常态 `IDLE` 是真实状态而非伪造。若要让它们生产自动触发，需在需求创建/变更影子钩子补接。
