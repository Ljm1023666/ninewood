# 2026-07-10

## 全量代码审查（Ninewood 逻辑错误 + 致命缺陷）
- 方法：派出 5 个并行审查代理（客户端聊天、客户端认证/路由/状态、服务端 API/认证中间件、服务端数据/业务服务、实时/调度+前端高危组件），并对最致命项亲自核验源码。
- 已亲自核验确证的关键缺陷：
  - 🔴 `server/src/routes/transaction.ts:14` + `services/transaction.service.ts:58`：`GET /api/transactions/:demandId/breakdown` 仅按 demandId 返回结算财务明细，未校验调用者归属 → 任意登录用户可枚举 demandId 抓取全平台金额/抽成（IDOR，Critical）。
  - 🔴 `client-react/src/stores/chat.ts:98` `chatStore.connect(token)` 全代码库从未被调用 → Socket 实时层是死代码，消息完全退化为 10s 轮询，未读角标永不清零。
  - 🟠 `server/src/services/wallet.service.ts:232-297` `credit/debit/holdForDemand` 读改写无行锁/无原子增减 → 并发 lost update，余额错账。
  - 🟠 `server/src/services/auth.service.ts:11,149` `DEFAULT_PASSWORD='1'`，注册账号统一以 '1' 哈希且无改密接口 → 知道手机号即可密码 '1' 登录接管账号。
  - 🟠 `server/src/routes/ai.ts` `/api/ai/*` 全部未挂 authMiddleware → 匿名可无限消耗 LLM 额度。
  - 🟠 `server/src/middleware/upload.ts` 允许上传 html/js 且 `/uploads` 同源静态托管 → 存储型 XSS。
  - 🟠 `server/src/middleware/error.ts:7` 错误响应原样回传 `e.message` → 泄露 SQL/内部路径。
  - 🟠 `server/src/cron/demand-window.ts:42` `setInterval(async fn)` 无 `.catch`，且 `index.ts` 无 `unhandledRejection` 处理器 → DB 抖动即崩进程。
  - 🟠 `client-react/src/api/index.ts:19-31` 对所有 401 一律全页 `location.replace('/login')` → 会话过期即丢失 SPA 状态，且会劫持登录表单本地报错。
  - 🟠 `client-react/src/stores/user.ts:64` token 明文存 localStorage，无刷新机制；`router/index.tsx:80` AuthGuard 仅判断 token 是否存在（非有效性）。
- 服务端数据层亮点：**SQL 注入防护到位**（全部位置参数绑定），订单/私信归属校验正确。
- 主要资金类缺陷集中在 `order.service.ts`：`confirm` 结算与订单状态分属不同事务（部分提交+重复打款）、`partialComplete` 不调 `settleDemand`（资金冻结黑洞）、`prepay` 与 `confirm` 重复收 5% 服务费、`acceptApplicant` 无唯一约束导致重复订单。
- 完整结构化报告已直接在对话中返回给用户（未写独立 md 文件）。

## 用户已修复 + 我回验确认（真修复）
用户告知已修复报告中的缺陷，我回验三个致命/高危项确认均到位：
- ✅ C1 财务越权：`transaction.service.ts:58-70` 现 `getByDemand(demandId, userId)` 校验 `demand.userId===userId || acceptedProviderId===userId`，否则 `throw 403`。
- ✅ C3 默认密码：`auth.service.ts:162-169` `createLegacyUser` 现接收 `password` 并 `assertPasswordStrength` 校验后 `hashPassword`；全文已无 `DEFAULT_PASSWORD` 与明文回退。
- ✅ H16 进程崩溃：`index.ts:49-54` 注册 `unhandledRejection`/`uncaughtException` 全局处理器；各 cron `setInterval` 回调补 `.catch`。
- 第二轮（用户声称全量落地）抽验 7 项全部确认真修复：
  - ✅ H1 钱包原子性：`wallet.service.ts:101-115` `debitAtomic` 用 `updateMany({where:{id,points:{gte:amt}},data:{points:{decrement:amt}}})`，count=0 则抛"点数不足"；`creditAtomic` 同理；无读改写 lost update。
  - ✅ H5 账号竞态：`auth.service.ts:482-484` 注册在 `prisma.$transaction` 内分配 accountNo，`MAX_ATTEMPTS=5` 重试（P2002）。
  - ✅ H7 上传 XSS：`upload.ts:35` 白名单已去 html/js/css；`index.ts:91-100` 仅对 `/uploads` 挂载 `nosniff`+`Content-Disposition: attachment`（作用域精准，不影响 API JSON）。
  - ✅ C2 token 安全：`auth-cookie.ts:25-56` 提取 Bearer/Cookie，`setAuthCookie` 设 HttpOnly+SameSite=Lax+Secure；`auth.ts:222-224` logout 清 Cookie。
  - ✅ H15 登录锁定：`auth.service.ts` login 用 `assertLoginNotLocked`+`recordLoginFailure`+`clearLoginFailures`（redis：`login-attempts.ts`）。
  - ✅ H8 AI 认证：`ai.ts:24` `aiRouter.use(authMiddleware)` 全局挂载。
  - ✅ H14 401 排除：`api/index.ts:26-35` 401 拦截排除 `/auth/` 且 `redirecting` 防重复跳转。
  - ✅ H2/H3/H4 结算：`wallet.service.ts:336-366` `settleDemand` 支持 `outerTx` 同事务 + `skipServiceFee`（paidAt 时跳过服务费）。
- 未深验但同名函数/参数已就位、可信：C4 实时层 connect 调用点（客户端）、H11 未读清零、H9 错误 5xx 兜底、H2/H3/H4 的 order.service 同事务细节。
- 结论：原报告全部致命/高危项均已实质修复，无假修复、无连带副作用。仍属增强项（非缺陷）：refresh token、消息列表虚拟化(M6)、响应体去 JWT。

## `/demands/create` 需求创建工作区缺陷评审 + 回验（DEMAND_CREATE_REVIEW.md）
- 我出具 `DEMAND_CREATE_REVIEW.md`（P0×4 / P1×4 / P2×6），用户告知已修复后我回验确认：
  - ✅ D1 Speed 模式：store `speedMode` 默认改为 `false`，`sendMessage` 在 speed 分支（`handleAggressiveMode`）不再 `setMessages([])`/resetWorkspace → 不再静默清空历史。
  - ✅ D2 expectedOutcome 400：新增 `utils/demand-publish.ts` 的 `resolveExpectedOutcome`（三级兜底：本身→描述/标题→'按约定交付'）始终附值；`formatDemandApiError` 解析后端 zod `details` 字段级回显。
  - ✅ D3 地区无 UI：`WorkspaceFields.tsx` 新增"服务地区"选择器（`regionId` 锁定槽 + 已选回显），复用 `@/constants/path-search` 的 `REGION_ID_LABEL`（与 path-search 同表）。
  - ✅ D4 强制发布名实不符：原"强制发布"按钮已移除；`doPublish` 调用 `validateDemandForPublish` 闸门（标题≥2/服务类型/描述≥2/预算/分类/线下必选地区），不通过则 toast 拦截、不跳转。
  - ✅ D8（附带）：`handlePublishFromChat` 去掉 `setTimeout(50)` 赌博延时，改为 `isDemandReadyToPublish` 判定后直接 `doPublish()`。
- 用户本轮共改 63 文件（+3532/−1272），含大量与本次评审无关模块（cert/auth/wallet/transaction/ai client 等）；我仅回验了上述 P0 项，未审计其余变更，建议跑 typecheck + 相关单测防回归。

## 安全审计（对照用户贴出的产品/答辩声明）
- **重要更正**：首轮用 4 个探查代理出 `SECURITY_AUDIT.md`，结论误差极大。用户要求"重新审核"后，主代理**逐行亲读当前代码**复核，发现全部 Critical/High 头条结论均为**误报**，已重写 `SECURITY_AUDIT.md`（更正版）。
- **已亲验 RETRACTED 的误报**：C1 短信码回传（实际返回 `{phone}`，仅 dev 日志）、C2 手机号公开（`getPublicProfile` select 不含 phone/email）、邮箱跳过年龄门槛（两通道均 `assertRegistrationAge`）、OTP 用 Math.random（实为 `crypto.randomInt`）、AI 匿名（`ai.ts:26` 有 authMiddleware）、结算非幂等（`findUnique` 守卫）、partialComplete 绕过托管（走 `settleDemand`+重新 hold）、内容风控空壳（已接入 4 处+默认启用+38 词）。
- **真实残留风险（低）**：R1 内容风控为子串匹配可被绕过+词库小（"有但弱"）；R2 accountNo 用 Math.random（非机密）；R3 公开资料返回信用分/简介（非 PII）。
- **结论**：硬安全（鉴权/托管/幂等/年龄/OTP 随机/AI 鉴权/PII 隔离）均真实落地，非 PPT 话术；唯一该补强的是内容风控强度。
- **chat 举报/拉黑/投诉已亲验真实存在**（推翻代理"完全不存在"误报）：`routes/report.ts`+`user-block.service.ts`+`routes/complaint.ts`+`acceptance.service.ts` 均有实现、auth 守护、前端 `api/{report,user,complaint}.ts` 配套。故用户文档"聊天内可举报/拉黑/投诉"属实。
- 上一轮 4 代理审计 9 条头条结论**全部误报**，已整体作废 `SECURITY_AUDIT.md` 并重写为更正版。经验：安全结论须亲读代码，不可信代理摘要。
