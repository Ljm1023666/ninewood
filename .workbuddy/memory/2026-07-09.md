# 2026-07-09

- 18:36 调整顶部导航栏 `ScrollNavbar`（`client-react/src/components/ui/scroll-navigation-menu.tsx`）桌面端布局：将外层容器改为 `w-full`，内部 flex 改为 `justify-center`，使 Logo 与导航项整体在页面水平居中。TypeScript 检查通过。

## 重新评定检索体验（18:42）
- 上下文：另一 AI 改完，重读全链路 + Python urllib 实测（curl 中文被二次编码乱码，用 Python 绕开）。
- 🔴 bkt 彻底洗清“死维度”：家政 627 条按 bkt:price 全分布（6/421/114/53/33/0=627），筛选正常工作。之前误诊根因=测试探测格式错（bkt:0-500 应为 bkt:price=100_500）。
- 新增能力（基线→现在）：
  - `query-expansion.ts` 受控扩展白名单（打车/叫车→网约车+叫车，排除出租车/包车/租车/自驾租车；外包→财务/客服/人事外包）——实测“打车”status=hit，excludePaths 生效
  - `excludePaths` 检索层 NOT 过滤（searchByPaths 新增参数，路由 / 透传）
  - resolve 状态机 hit/partial/miss + suggestions
  - `region-aliases.ts` 大量城市/区县别名→regionId（rgn resolve 基础）
  - `jieba-segment.ts` 分词服务
  - 8 个测试文件（前后端 path-codec/resolver/search-api/query-expansion/region-resolver）
  - `backfill-demand-paths.ts` / `seed-coverage-gaps.ts` / `audit-demand-paths.ts` 等运维脚本
- 仍存的缺口（未修）：
  - 🔴 rgn 前端无地区选择器（PathSearchControls 仍只有 attr+bkt；后端全通、resolve 识别“北京”、筛选 total=16 生效，但 UI 没入口）
  - 🐛 /coverage 对 facet 路径(bkt/rgn/attr)返回 {}，computePathCoverage 只 validateScoringPaths
  - 🟡 bkt resolve 不识别“预算500”自然语言（facets=[]，只能下拉触发）
  - 🟡 intent 长匹配偏置（query-expansion 仅白名单词如“打车”，未解决通用复合意图如“王者荣耀代练”）
- 数据变化：家政基准 664→627、北京家政 17→16（seed/backfill 调整）。
- 产物：`path-search-retrieval-report.md` 已按 7-09 重新评定全面更新（推翻 bkt 误诊、补新增能力、重排待办）。
- 修复聊天气泡放大后不靠右的问题（`client-react/src/styles/messages-chat.css`）：为 `.msg-row` 和 `.msg-row__inner` 显式添加 `width: 100%`，避免页面缩放后消息行 flex 容器缩为内容宽度，导致右侧气泡偏离聊天区域右边缘。
- 进一步移除 `.msg-thread` 的 `max-width: 840px` 与 `margin: 0 auto`，让消息区随聊天区域宽度自适应，解决页面加宽后气泡居中定死、不跟随页面变宽的问题。
- 消息页输入框改为 Codex 风格浮空样式（`client-react/src/styles/messages-chat.css`）：`.msg-composer` 透明背景并底部留白，`.msg-composer__bar` 改为居中、最大宽度 720px、大圆角、带阴影的悬浮卡片，输入框无边框透明背景，发送按钮激活态改为实心主题色圆形。
- 未选择聊天时隐藏输入框（`client-react/src/components/ui/chat-template.tsx` 的 `DefaultRightChatPanel`，以及 `client-react/src/views/ChatDetail.tsx` 的 `hasChat` 条件），避免空状态出现悬浮输入框。

- 19:00 起优化 Admin Dashboard（`client-react/src/views/admin/*`、`client-react/src/views/admin/admin-ui.tsx`、`client-react/src/index.css`）：
  - 设计系统：圆角半径从 0 提升到 14px/10px/6px，增加三级柔和阴影，统一过渡曲线（0.25s ease），字体缩小到 15px 基础、Inter 主字体。
  - 侧栏：激活项增加左侧 3px 蓝色指示条，按钮悬浮轻微右移；用户卡片加圆角与悬浮效果。
  - 顶栏/子导航：标题字号降级、badge 改为圆角胶囊、刷新按钮加阴影与悬浮提升；子导航按钮增加悬浮背景与顶部圆角。
  - 通用组件：Metric/Panel/Chart/List/StatusBadge/SearchInput 全部改为卡片式间隙布局，带圆角、阴影、悬浮提升与平滑过渡。
  - 各 Tab 图表：统一使用新蓝色 #2563EB，柱状图圆角加大；补齐 Users/Orders/Disputes/System/Analytics 的原始 loading 骨架与表单控件样式。
  - 为 dashboard 内容区增加错落进入动画（fade-up + 60ms 递增延迟）。
  - TypeScript 检查通过。

## TASK-11 路径检索全量修复（按 handoff 文档落地）
- 范围：bkt 数据层已健康，未动 derivePaths/backfill。
- P0 前端地区下拉：
  - `client-react/src/constants/path-search.ts`：新增 `REGION_FACET_OPTIONS`（复制 server REGION_ENTRIES 的市级 id+主名，value=`rgn:<id>`）、`REGION_ID_LABEL`(id→名)；增强 `formatPathDisplay`（`rgn:110000`→`地区 · 北京`，`bkt:price=100_500`→`预算 · ¥100–500`）。
  - `client-react/src/views/path-search/PathSearchControls.tsx`：接入地区 `<select>`，复用 `.psa-controls__pricesel` 样式，`replaceFacetOfType(facets,'rgn',v)`，aria-label="地区"，图标 location_on。后端零改动。
- P1 `/coverage` facet 空返回：
  - `server/src/services/path-search.ts` 的 `computePathCoverage` 改为 `splitQueryInputs` 后合并 `validateScoringPaths`+`validateFacets` 再 COUNT（方案 A）；`searchByPaths` 内部调用仍只传 scoring（不变）。
  - `server/src/routes/path-search.ts` 的 `/coverage` 路由传入 `[...scoringPaths, ...facets]`。
- P1 bkt 自然语言 resolve：
  - 新建 `server/src/services/price-facet-resolve.ts`（`priceFacetFromText`/`priceFacetRaw`，识别「预算500/500块/价格1000以内/budget 500/一千以内/八千以内」→ `bkt:price=${priceToBucket}`，含中文数字与全角归一，范围 1–999999）。
  - `path-resolver.ts` 的 `resolveInputFacets` token/segment 循环接入；vocab 循环跳过 `type==='bkt'` 防数字误挂错桶。
- P2 可选（一并实现）：
  - `resolveIntentPaths` 复合意图：4+ 纯中文长片段（如「王者荣耀代练」）按动作后缀(代练/陪玩/…)拆前缀+后缀取池内 tag/kw（上限2）。
  - `path-search-query.ts` 的 `parseSearchQuery` 当 `intentMatch!=='off'` 且无 q 时降级为 `'off'`（不再抛 400）；同步更新 `path-search-api.test.ts` 与 `path-search-query.test.ts` 断言。
- 验证：`npm run typecheck`（server+client）全过；`pnpm --filter server run test -- path-codec path-resolver path-search-api price-facet-resolve region-resolver` 全绿（336 tests，37 files）；前端两改动文件单独 eslint EXIT=0（全量 lint 的 58 error 均为无关文件的既有问题）。本环境无 DB（localhost:5432 不通），B1–B7 实测由验收 AI 对运行服务执行。
- 注意：handoff 的 `npm -w server` 写法在本 pnpm workspace 不适用，正确命令为 `pnpm --filter server run test -- …`；`vitest run` 已是单次运行，无需 `--run`。

## 全仓安全/逻辑审查（14:08，5 代理 + 亲验）

**统计**：Critical 4 · High 16 · Medium 9 · Low 8

**亲验 Critical**：
- **C1** `transaction.ts:12-14` → `getByDemand(demandId)` 无归属校验，任意登录用户可读全平台订单财务拆分（IDOR）。
- **C3** `auth.service.ts:11` `DEFAULT_PASSWORD='1'`，注册统一哈希 `'1'`；无 set-password 端点；login 有明文回退。
- **C4** `chat.ts` `connect()` 全仓未调用，Socket 实时层失效，退化为轮询。
- **H1** `wallet.service.ts` credit/debit 读改写无原子增减/行锁。

**建议止血顺序**：C1 → C3 → H7/H8/H9 → C4+H11+H12 → H1/H2/H3/H4/H5/H6 → H14/H15/C2 → H16。

**2026-07-10 14:37 第四批（收尾）已落地**：
- M1 `/settings/data` 纳入 AuthGuard
- M8 `getConversations` 有界 + groupBy 批量未读（去 N+1）
- M10 push `findMany` take 500
- M11 review 评分整数 1–5 + 内容 ≤2000
- M12 captcha 需 `CAPTCHA_DEV_BYPASS=1` 才允许 dev-bypass
- M3 BroadcastChannel 跨标签登录同步；M4 消息上限 400；M7 ChatDetail 细粒度订阅
- L1 socket wire 具名 off；L3 partialComplete 价格校验；L4 favorite 按 id 回滚；L8 `$queryRaw` 模板
- uploads `Content-Disposition: attachment` + nosniff；Vite 代理转发 Cookie
- 刷新后 Cookie 会话仍 connect Socket（不依赖内存 token）
- 验证：typecheck 通过，342 tests 全绿
- 未做：M6 消息列表虚拟化（大改 UI，非止血项）

- C1 `transaction.service.getByDemand` 归属校验 + 测试
- C3 注册强制密码≥6、删明文回退、邮箱用户随机密码
- C4 `Layout`/`userStore` 接入 `chatStore.connect`
- H8 `/api/ai` 全局 `authMiddleware`
- H9 生产 5xx 泛化错误信息
- H16 `demand-window` cron `.catch` + `unhandledRejection` 守卫

**亮点**：SQL 参数化到位；order/message 归属校验正确；JWT verify 健全；聊天无 dangerouslySetInnerHTML。

