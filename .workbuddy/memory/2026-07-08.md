# 2026-07-08

## 测试 path-search 检索能力
- 环境：前端 localhost:5174 代理 /api → 后端 localhost:3001（Express + Prisma/Postgres）。
- 用 Python+urllib 直接打后端 API（curl -G 中文会被代理二次编码成乱码，故绕开）。
- 结论：resolve（自然语言/显式前缀/facet 抽取/判空）与 search（any/all/custom 交叉命中、7 种排序、intentMatch 带 q、attr 筛选、分页/coverage）主链路健壮准确。
- 发现的问题：
  1. 🔴 bkt(预算)/rgn(地区) 筛选维度在种子数据里无覆盖（coverage 全空，resolve 也不生成），UI 预算/地区筛选是死维度。需确认 derivePaths 是否写入 minPrice/regionId 到 demand.paths。
  2. 🟡 intent 路径长匹配偏置：「王者荣耀代练」只取 tag:王者荣耀 漏掉 代练。
  3. 🟢 segment 泄漏停用短语（我想/找个）；intentMatch 无 q 时 400（前端总带 q 不触发）。
- 产物：scripts/test-path-search-retrieval.py、path-search-retrieval-report.md。

## 重新测试 path-search（14:55）
- 对比首测，地区(rgn)维度已复活：resolve "北京"→`rgn:110000` facet（不再误当 kw:北京）；搜索 `家政+rgn:110000` 把 664 筛到 17，需求真实挂 rgn 路径（长沙/成都/佛山等多城市）。
- 预算(bkt)维度仍死：resolve 不产生 bkt，池内无 bkt 路径，coverage 全空。
- 新发现 bug：`/coverage` 端点对 facet 类型路径(rgn/bkt/attr)返回空 {}，因 parseSearchPathInputs 把 facet 路由走、computePathCoverage 只 validateScoringPaths 丢弃；但搜索筛选本身正确。
- 未变项：intent 长匹配偏置（王者荣耀代练漏代练）、segment 噪声、intentMatch 无 q 时 400。
- 报告已更新 path-search-retrieval-report.md。

## 分析项目核心功能（15:03）
- 通读 README/PRODUCT/DESIGN/CLAUDE/DEPLOY + docs（ENGINEERING_OVERVIEW、FEATURE_SPECIFICATIONS）+ 路由与视图清单。
- 定位：Windows 桌面优先的本地生活服务撮合平台（威客/兼职），核心闭环为「发布需求 → 抢单 → 履约 → 评价」。
- 三层架构：表现层(React19+TS+Electron 8 模块) / 接口层(Express 38 routes REST+SSE+WS) / 服务层(Prisma+PG / Socket.IO / Redis / MiniMax AI)。
- 八大功能模块：发现卡池、需求订单(两段式接单+点数担保)、消息圈子、钱包担保、认证信用、市场分析、管理后台、AI 助手(Agent 工具+Skill)。
- 输出：内联架构图 + 结构化文字拆解（未落盘文件）。

## 评估检索体验现状（15:08）
- 两套检索系统易混淆：Search.tsx（找人，userApi.search 昵称/手机号）vs PathSearchPage（路径检索引擎，path-search API），入口命名都叫"搜索"。
- PathSearchPage 体验精致：极光背景 + 路径 chips（圆点区分意图/分类/语义/筛选/金银双态）+ 命中环形图 MetricRing + 缺失意图标记 + "同分优先"丝带 + URL 状态同步 + compact 响应式收纳 + 颜色图标说明弹窗。品牌名"极光神经·路径引擎"。
- 筛选维度 UI（PathSearchControls）：服务方式 attr✅ / 价格档 bkt⚠️死 / 命中要求 match+minHit / 意图要求 intentMatch / 排序。
- 体验问题（已修正）：①bkt 之前误诊为"死维度"——根因是测试探测格式错（用 bkt:0-500，应为 bkt:price=100_500）；derivePaths(path-codec.ts:119) 无条件写 bkt:price=<bucket>，前端 PRICE_FACET_OPTIONS 格式匹配，UI 下拉筛选应能工作；真实缺陷是 resolve 不识别"预算500"等自然语言。②rgn 与 bkt 是相反缺陷：rgn 数据✅+resolve识别"北京"✅但前端无地区选择器；bkt 数据✅+UI下拉✅但resolve不识别"预算"自然语言。③/coverage bug 确认：computePathCoverage 只 validateScoringPaths，facet 类型被拒；路由也只传 scoringPaths。④intent 长匹配偏置；⑤segment 噪声；⑥intentMatch 无 q 时 400。
- 待办：用正确格式 bkt:price=100_500 重测 coverage 确认池内真有 bkt 数据（derivePaths 会写但取决于数据是否经此流程）。

## TASK-11 路径检索池覆盖与意图精度改造（15:26–16:13）
按 docs/specs/TASK-11-path-pool-coverage.md + seed-data/path-coverage-manifest.json 全量实施，先补数据再改解析。
- **P0 数据补种**：新建 `server/scripts/seed-coverage-gaps.ts`（参考 seed-car-rental-supplement.ts，幂等按 manifest 补缺口）。实测补 财务外包+8、客服外包+8、网约车+10、叫车+10（人事外包不在 manifest，跳过）；其余词池内已满足自动跳过。跑 `backfill-demand-paths.ts --force` 全量重算 10133 条需求 paths（EXIT=0）。
- **P1 解析层**：
  - 新建 `server/config/path-substring-rules.json`（DISTINCT_SUBSTRING_PAIRS 配置化，租车|出租车，加载时按长度归一 shorter|longer，文件缺失回退内置默认）。
  - 新建 `server/src/services/query-expansion.ts`（受控白名单 QUERY_PATH_EXPANSIONS：打车/叫车/网约车→网约车+叫车 并排除 出租车/包车/租车/自驾租车；外包→财务/客服/人事外包。triggers 经 normalizeValue 精确匹配，不做模糊扩散）。
  - 改 `path-resolver.ts`：从 SCORING_SYNONYMS 移除 打车→出租车（改由扩展提供）；resolveInputToPaths/resolveIntentPaths 集成扩展（追加 expandTags 不跳过模糊匹配，仅池内存在的 tag 才挂载）；resolveInputFull 返回新增 excludePaths/suggestions/status；computeUnresolvedSegments 识别扩展触发词为已解析（避免误判 partial）；computeSuggestions 为 miss/partial 推荐池内近似路径。
  - 改 `path-search.ts`：resolveQueryToPaths 透出 status/suggestions/excludePaths；searchByPaths 增加 excludePaths 参数（SQL `AND NOT (d.paths && exclude)`），meta 暴露 excludePaths。
  - 改 `routes/path-search.ts`：/  接受 excludePaths 查询参数。
- **P2 治理脚本**：新建 `check-path-coverage.ts`（CI 门禁，非零退出：requiredPaths 存在+minActiveDemands+requiredFacets+excludePaths 数据卫生「租车需求不得同时挂出租车」）；新建 `audit-demand-paths.ts`（只读：tag/paths 一致性、租车/出租车混标、空 paths、manifest exclude 泄漏）。实测全清（0 不一致/0 混标/0 空/0 泄漏）。
- **P2 前端**：改 client api/constants（PathResolveResult+PathSearchMeta 增 excludePaths/suggestions/status，ResolveStatus 类型+标签，excludePaths URL 序列化）；改 PathSearchPage.tsx（excludePaths 经 URL 持久化并传 search；打车/排除供给时显示提示条 psa-hintbar；miss 时展示建议面板 psa-suggest-panel；handleSearch 接受 overrideQ 解决 setQuery 异步时序）；补 path-search-aurora.css 样式（含 light 主题）。
- **词表同步**：tags-vocabulary.json +8 标签（网约车/叫车/打车/租车/出租车/包车/自驾租车/人事外包，1170 总）；generate_massive_seed.py 同步 TAG_GROUPS + 汽车/企业服务模板（网约车/叫车/打车/包车 + 财务外包/人事外包 模板），py_compile 通过。
- **验收全绿**：resolver 单测 18/18（region-resolver 打车断言改为 网约车/叫车 + 新增 excludePaths 断言）；全量 server 测试 303/303；server typecheck 通过；check-path-coverage 10/10；§6 诊断（外包/打车/租车/南京租车）全部 status=hit 且路径正确，打车结果无 出租车/租车 混入。client typecheck 仅剩既有无关文件错误（MyDemands/Settings/tax-visualizer）。
- 关键设计决策：扩展「追加不替换」（保留 tag:打车 同词命中）；excludePaths 不计入 perQuery 上限（独立校验，最多 20 条）；受控扩展保证 打车≠出租车 不靠模糊子串（二者无子串关系，必须显式白名单）。

